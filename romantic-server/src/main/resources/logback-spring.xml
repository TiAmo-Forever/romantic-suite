<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- 读取应用名称、日志目录和日志格式配置。 -->
    <springProperty scope="context" name="APP_NAME" source="spring.application.name" defaultValue="application"/>
    <springProperty scope="context" name="LOG_PATH" source="logging.path" defaultValue="./log"/>
    <springProperty scope="context" name="PATTERN_CONSOLE" source="logging.pattern.console"
                    defaultValue="%d{yyyy-MM-dd HH:mm:ss} %-5level [%thread] [%X{tid}] %-20.20logger{20} - %msg%n"/>
    <springProperty scope="context" name="PATTERN_FILE" source="logging.pattern.file"
                    defaultValue="%d{yyyy-MM-dd HH:mm:ss} %-5level [%thread] [%X{tid}] %logger{36} - %msg%n"/>

    <!-- 控制台输出。 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${PATTERN_CONSOLE}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- 文件输出，当前文件固定为 log/romantic-server.log，按天并结合 10MB 大小滚动。 -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/${APP_NAME}.log</file>
        <encoder>
            <pattern>${PATTERN_FILE}</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>2GB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

</configuration>
